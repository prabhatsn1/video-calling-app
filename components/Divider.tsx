import { View, Text } from "react-native";

const Divider = () => {
  return (
    <View
      style={{
        borderBottomColor: "white",
        borderBottomWidth: 1,
        marginVertical: 20,
      }}
    />
  );
};

export default Divider;
